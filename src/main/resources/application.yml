server:
  port: 8081
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/admin?useUnicode=true&useSSL=false&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: root
  security:
    user:
      name: user
      password: 123
  redis:
    host: 127.0.0.1
    database: 1
    port: 6379
mybatis-plus:
  mapper-locations: classpath*:/mapper/**Mapper.xml
